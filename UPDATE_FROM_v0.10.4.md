# B5 SST Player v0.10.5 - update from v0.10.4

## 変更内容

### 1. ナレーション・セリフ・心の声を明確に分離
物語モードのサウンドノベル領域で、行の種類に応じて表示を変えるようにした。

- ナレーション：青系の文字・青系の上罫線
- 登場人物のセリフ：白い本文・金系の話者ラベル
- 心の声：紫系の本文・紫系の話者ラベル

`kind` がない旧ストーリーも、speaker/textから自動判定するため後方互換。

### 2. transition内のセリフ分離に対応
`transitions[].lines[]` が従来の文字列だけでなく、`speaker/text/kind` を持つオブジェクト形式でも進行できるようにした。
これにより、つなぎ場面で「ナレーションの中にセリフが埋め込まれる」状態を解消できる。

### 3. ルビ表示は既存 `[[漢字|かな]]` を継続
Playerの既存Rubyエンジンを維持し、漢字＋ルビの本文をそのままサウンドノベル表示・選択肢・コーチ画面へ反映する。

### 4. endingの将来スキルをストーリー固有化
第1話固定の文章を廃止し、`story.meta.futureSummary` を表示するよう修正。

### 5. loader検査を追加
つなぎの各行が、文字列または `text` を持つオブジェクトであることを検査する。
