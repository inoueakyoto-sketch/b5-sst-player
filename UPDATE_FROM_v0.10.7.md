# B5 SST Player v0.10.8

## 変更点
- ひらがなだけで表示されていた頻出語を、小学4年生程度までの身近な漢字を中心に自動変換。
- Playerが自動で追加する漢字はすべてルビ付き（`[[漢字|よみ]]` と同じ出力）。
- ストーリー側ですでに指定されているルビを最優先し、二重変換しない。
- ナレーション、セリフ、選択肢、フィードバック等のルビ表示に合わせて行間を拡大。
- 低い画面では行間を少し圧縮しつつ、ルビと本文が重ならない値を維持。

## 更新対象
`ruby.js`, `styles.css`, `version.js`
