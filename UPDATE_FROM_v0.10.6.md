# B5 SST Player v0.10.7 — v0.10.6 からの更新

## 変更点
- ボタン内の長文・ルビで文字が重ならないよう、ボタンを内容に応じて伸びるレイアウトへ変更。
- 選択肢番号と本文を2列グリッド化し、「みた」バッジを通常フローに移して本文への重なりを防止。
- ページ送りのボタンとページ表示をグリッド配置にして、長いボタン文言でも本文に重ならないよう調整。
- SSTコーチ画面と上部進捗UIの間隔を広げ、パネル上端の干渉を防止。
- ナレーション本文を明るい水色、登場人物のセリフ本文を白、心の声を薄紫に固定し、本文色そのものを明確に区別。
- 物語本文、相手の反応、ポイント、言い方の例、振り返り、エンディングの最小文字サイズを引き上げ。
- 高さ520px以下の横画面でも文字を極端に縮小せず、必要時はパネル内スクロールを使う方針へ変更。

## 互換性
`.sststory` の形式は変更していません。v0.10.6で読み込めるストーリーはそのまま利用できます。
